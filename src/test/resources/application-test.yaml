# Reference - https://ebean.io/docs/testing
ebean:
  test:
    platform: h2 # h2, postgres, mysql, mariadb, nuodb, sqlserver, oracle, hana, clickhouse, cockroach, sqlite
    ddlMode: dropCreate # none | dropCreate |  migrations
    dbName: test
